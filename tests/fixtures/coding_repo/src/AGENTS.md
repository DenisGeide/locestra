# Source rules

- Keep source changes minimal and compatible with the Python standard library.
- Do not weaken or delete regression tests to make a failure disappear.
- Security fixes must remove the unsafe data flow instead of hiding a warning.
